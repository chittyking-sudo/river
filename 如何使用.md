# 🎮 银河竞赛 - Cocos Creator & 微信小游戏版本使用指南

## 🎯 版本选择

根据您的需求选择合适的版本：

### 选择 Cocos Creator 版本，如果您需要：
- ✅ 跨平台发布（Web、iOS、Android）
- ✅ 可视化编辑器开发
- ✅ TypeScript类型安全
- ✅ 组件化开发模式
- ✅ 更强大的物理和渲染引擎

### 选择微信小游戏版本，如果您需要：
- ✅ 仅在微信中运行
- ✅ 更小的包体积
- ✅ 更快的启动速度
- ✅ 原生微信社交功能（分享、授权等）
- ✅ 简单的Canvas绘制

## 📁 项目文件位置

```
webapp/
├── cocos-version/          ← Cocos Creator版本在这里
├── wechat-minigame/        ← 微信小游戏版本在这里
└── COCOS_WECHAT_README.md  ← 两版本对比文档
```

## 🚀 快速开始

### 方案A：使用 Cocos Creator 版本

#### 步骤1：安装Cocos Creator
1. 前往 [Cocos Creator官网](https://www.cocos.com/creator)
2. 下载并安装 Cocos Creator 3.8.0 或更高版本
3. 注册并登录Cocos账号

#### 步骤2：打开项目
1. 启动 Cocos Creator
2. 点击"打开其他项目"
3. 选择 `webapp/cocos-version/` 文件夹
4. 等待项目资源加载完成

#### 步骤3：预览游戏
1. 在顶部工具栏点击"运行"按钮（▶️）
2. 选择"浏览器"作为预览平台
3. 游戏将在浏览器中打开

#### 步骤4：构建发布
1. 点击菜单 "项目" → "构建发布"
2. 选择目标平台：
   - **Web Mobile**: 移动端网页
   - **Web Desktop**: 桌面端网页
   - **Android**: 安卓应用
   - **iOS**: iOS应用
3. 配置发布参数
4. 点击"构建"按钮
5. 构建完成后在`build/`目录找到输出文件

#### 开发建议
```typescript
// 在 Cocos Creator 中创建自定义组件
import { _decorator, Component } from 'cc';
import { GameController } from './controllers/GameController';

const { ccclass, property } = _decorator;

@ccclass('MyComponent')
export class MyComponent extends Component {
    onLoad() {
        // 初始化代码
    }
    
    update(deltaTime: number) {
        // 每帧更新
    }
}
```

详细文档：[cocos-version/README.md](./cocos-version/README.md)

---

### 方案B：使用微信小游戏版本

#### 步骤1：安装微信开发者工具
1. 前往 [微信开放平台](https://developers.weixin.qq.com/minigame/dev/devtools/download.html)
2. 下载"微信开发者工具"
3. 安装并启动

#### 步骤2：创建小游戏项目
1. 打开微信开发者工具
2. 点击"小游戏"
3. 点击"导入项目"
4. 选择 `webapp/wechat-minigame/` 文件夹
5. AppID选择"测试号"（或填写您的AppID）
6. 点击"导入"

#### 步骤3：运行游戏
1. 项目导入后会自动编译
2. 在左侧模拟器中预览效果
3. 点击"编译"按钮重新编译

#### 步骤4：真机调试
1. 点击顶部工具栏的"真机调试"
2. 用微信扫描二维码
3. 在手机上查看实际效果

#### 步骤5：发布上线
1. 确保填写了正式AppID
2. 完善游戏信息和配置
3. 点击"上传"按钮
4. 设置版本号和备注
5. 登录[微信公众平台](https://mp.weixin.qq.com/)
6. 在"版本管理"中提交审核
7. 审核通过后点击"发布"

#### 开发建议
```javascript
// 在微信小游戏中使用Canvas绘制
const canvas = wx.createCanvas();
const ctx = canvas.getContext('2d');

// 绘制游戏内容
function render() {
    // 清空画布
    ctx.fillStyle = '#000000';
    ctx.fillRect(0, 0, canvas.width, canvas.height);
    
    // 绘制文本
    ctx.fillStyle = '#FFFFFF';
    ctx.font = '24px Arial';
    ctx.fillText('银河竞赛', 100, 100);
}

// 游戏循环
function gameLoop() {
    render();
    requestAnimationFrame(gameLoop);
}

gameLoop();
```

详细文档：[wechat-minigame/README.md](./wechat-minigame/README.md)

---

## 🎨 核心功能使用

### 1. 创建玩家

#### Cocos Creator版本
```typescript
import { GameController } from './controllers/GameController';
import { AvatarConfig } from './models/AvatarConfig';

// 获取GameController实例
const gameController = this.node.getComponent(GameController);

// 创建头像配置
const avatarConfig = new AvatarConfig(
    0,  // 基础形象: 0-2
    1,  // 配饰: 0-2
    2   // 颜色主题: 0-2
);

// 创建玩家
const player = gameController.createPlayer(avatarConfig);
console.log('玩家已创建:', player);
```

#### 微信小游戏版本
```javascript
// 创建头像配置
const avatarConfig = {
    baseAvatar: 0,   // 基础形象: 0-2
    accessory: 1,    // 配饰: 0-2
    colorTheme: 2    // 颜色主题: 0-2
};

// 创建玩家
const player = gameController.createPlayer(avatarConfig);
console.log('玩家已创建:', player);
```

### 2. 创建组队

#### Cocos Creator版本
```typescript
import { GroupDirection } from './models/Group';

// 创建向上冲击的组队
const group = gameController.createGroup(
    playerId,              // 玩家ID
    GroupDirection.UP      // 冲击方向
);

// 加入已有组队
const success = gameController.joinGroup(playerId, groupId);
```

#### 微信小游戏版本
```javascript
// 创建向上冲击的组队（0=UP, 1=DOWN）
const group = gameController.createGroup(playerId, 0);

// 加入已有组队
const success = gameController.joinGroup(playerId, groupId);
```

### 3. 执行组队冲击

```javascript
// 两个版本用法相同
const success = gameController.executeGroupImpact(groupId);
if (success) {
    console.log('冲击成功！');
}
```

### 4. 融合救援

```javascript
// 两个版本用法相同
const mergedPlayer = gameController.mergeRescue(playerId1, playerId2);
if (mergedPlayer) {
    console.log('融合成功:', mergedPlayer);
}
```

### 5. 获取游戏状态

```javascript
// 两个版本用法相同
const gameState = gameController.getGameState();
console.log('玩家数量:', gameState.players.length);
console.log('当前市场模式:', gameState.currentMode);
console.log('中位数分数:', gameState.medianScore);
```

## 🔧 开发调试

### Cocos Creator调试

1. **使用Chrome开发者工具**
   - 在浏览器预览时按F12打开开发者工具
   - 在"Sources"面板设置断点
   - 使用console.log输出信息

2. **使用Cocos Creator调试器**
   - 点击菜单"开发者" → "开发者工具"
   - 查看场景层级和节点属性
   - 监控性能指标

### 微信小游戏调试

1. **使用模拟器调试**
   - 在微信开发者工具中查看console输出
   - 使用"调试器"面板设置断点

2. **真机调试**
   - 点击"真机调试"扫码
   - 查看vconsole输出
   - 使用chrome://inspect调试

## 📊 性能优化建议

### Cocos Creator
- 使用对象池管理频繁创建的对象
- 合理使用节点缓存
- 使用纹理图集减少DrawCall
- 避免在update中进行复杂计算

### 微信小游戏
- 使用离屏Canvas预渲染
- 实现对象池复用机制
- 只在数据变化时重绘
- 优化触摸事件处理

## 🆘 常见问题

### Q1: Cocos Creator导入项目后场景是空的？
**A**: 这是正常的，当前提供的是代码框架。您需要：
1. 在场景中创建节点
2. 挂载GameController组件
3. 创建UI界面
4. 绑定数据和事件

### Q2: 微信小游戏显示白屏？
**A**: 检查以下几点：
1. game.js是否正确导入了game-adapter
2. console是否有错误信息
3. Canvas是否正确创建
4. 确认requestAnimationFrame正常工作

### Q3: 如何添加自定义UI？
**A**: 
- **Cocos Creator**: 在编辑器中创建UI节点，编写Component组件
- **微信小游戏**: 在Canvas上使用绘制API手动绘制UI

### Q4: 数据如何保存？
**A**: 两个版本都使用StorageService：
```javascript
// 保存游戏状态
StorageService.saveGameState(gameState);

// 加载游戏状态
const state = StorageService.loadGameState();
```

### Q5: 如何添加音效？
**A**:
- **Cocos Creator**: 使用AudioSource组件
- **微信小游戏**: 使用wx.createInnerAudioContext()

## 📚 进一步学习

### Cocos Creator资源
- [官方文档](https://docs.cocos.com/creator/manual/zh/)
- [API参考](https://docs.cocos.com/creator/api/zh/)
- [社区论坛](https://forum.cocos.org/)

### 微信小游戏资源
- [官方文档](https://developers.weixin.qq.com/minigame/dev/guide/)
- [API文档](https://developers.weixin.qq.com/minigame/dev/api/)
- [开发者社区](https://developers.weixin.qq.com/community/minigame)

## 🤝 获取帮助

如有问题，请：
1. 查看详细文档：[COCOS_WECHAT_README.md](./COCOS_WECHAT_README.md)
2. 查看GitHub Issues
3. 提交Pull Request

---

**祝您开发顺利！🎉**
