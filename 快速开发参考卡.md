# 🚀 银河竞赛 - 快速开发参考卡

## 📁 项目结构速查

```
webapp/
├── cocos-version/          # Cocos Creator版本
│   └── assets/scripts/
│       ├── models/         # 数据模型（已完成）
│       ├── services/       # 服务层（已完成）
│       ├── controllers/    # 控制器（已完成）
│       └── ui/            # UI组件（待实现）⭐
│
├── wechat-minigame/       # 微信小游戏版本
│   ├── js/
│   │   ├── models/        # 数据模型（已完成）
│   │   ├── services/      # 服务层（已完成）
│   │   ├── controllers/   # 控制器（已完成）
│   │   ├── ui/           # UI组件（待实现）⭐
│   │   └── managers/     # 管理器（待实现）⭐
│   └── game-adapter/     # 微信适配器（已完成）
│
└── 文档/
    ├── UI视觉音效添加指南.md        # 详细实现指南 ⭐
    ├── 资源制作和实现路线图.md       # 开发路线图 ⭐
    ├── COCOS_WECHAT_README.md      # 版本对比
    └── 如何使用.md                  # 快速上手
```

---

## 🎯 核心API速查

### Cocos Creator

#### 场景管理
```typescript
import { director } from 'cc';

// 加载场景
director.loadScene('MainGame');

// 预加载场景
director.preloadScene('MainGame', () => {
    console.log('预加载完成');
});
```

#### UI组件基础
```typescript
import { _decorator, Component, Label, Button } from 'cc';
const { ccclass, property } = _decorator;

@ccclass('MyUI')
export class MyUI extends Component {
    @property(Label)
    label: Label = null;
    
    @property(Button)
    button: Button = null;
    
    onLoad() {
        this.button.node.on('click', this.onClick, this);
    }
    
    onClick() {
        this.label.string = 'Clicked!';
    }
}
```

#### 音频播放
```typescript
import { AudioManager } from './managers/AudioManager';

// 播放BGM
AudioManager.getInstance().playBgm('main_theme');

// 播放音效
AudioManager.getInstance().playSfx('click');
```

#### 动画
```typescript
import { tween, Vec3 } from 'cc';

// 移动动画
tween(node)
    .to(0.5, { position: new Vec3(100, 100, 0) })
    .start();

// 缩放动画
tween(node)
    .to(0.3, { scale: new Vec3(1.2, 1.2, 1) })
    .to(0.3, { scale: new Vec3(1, 1, 1) })
    .start();
```

---

### 微信小游戏

#### Canvas绘制
```javascript
const canvas = wx.createCanvas();
const ctx = canvas.getContext('2d');

// 清空画布
ctx.fillStyle = '#000000';
ctx.fillRect(0, 0, canvas.width, canvas.height);

// 绘制文本
ctx.fillStyle = '#FFFFFF';
ctx.font = '24px Arial';
ctx.textAlign = 'center';
ctx.fillText('Hello', 100, 100);

// 绘制矩形
ctx.fillStyle = '#FFD700';
ctx.fillRect(50, 50, 200, 60);

// 绘制圆形
ctx.beginPath();
ctx.arc(100, 100, 20, 0, Math.PI * 2);
ctx.fill();
```

#### 触摸事件
```javascript
wx.onTouchStart((event) => {
    const touch = event.touches[0];
    console.log('触摸开始:', touch.clientX, touch.clientY);
});

wx.onTouchEnd((event) => {
    const touch = event.changedTouches[0];
    console.log('触摸结束:', touch.clientX, touch.clientY);
});
```

#### 音频播放
```javascript
// BGM
const bgm = wx.createInnerAudioContext();
bgm.src = 'assets/audio/bgm/main_theme.mp3';
bgm.loop = true;
bgm.play();

// 音效
function playSfx(src) {
    const audio = wx.createInnerAudioContext();
    audio.src = src;
    audio.play();
    audio.onEnded(() => audio.destroy());
}

playSfx('assets/audio/sfx/click.wav');
```

#### 本地存储
```javascript
// 保存数据
wx.setStorageSync('key', { data: 'value' });

// 读取数据
const data = wx.getStorageSync('key');

// 异步方式
wx.setStorage({
    key: 'key',
    data: { data: 'value' },
    success: () => console.log('保存成功')
});
```

---

## 🎨 UI组件实现清单

### P0 - 必须实现（36小时）

#### ✅ Cocos Creator
```typescript
// 1. StartupUI.ts - 启动界面（4小时）
@ccclass('StartupUI')
export class StartupUI extends Component {
    @property(ProgressBar) loadingBar: ProgressBar;
    @property(Button) startButton: Button;
}

// 2. AvatarCreationUI.ts - 头像创建（8小时）
@ccclass('AvatarCreationUI')
export class AvatarCreationUI extends Component {
    @property(Label) stepIndicator: Label;
    @property(Node) optionsContainer: Node;
    private currentStep: number = 0;
    private selections: number[] = [0, 0, 0];
}

// 3. GalaxyViewUI.ts - 银河视图（12小时）
@ccclass('GalaxyViewUI')
export class GalaxyViewUI extends Component {
    @property(Node) starsContainer: Node;
    @property(Prefab) playerDotPrefab: Prefab;
    private playerDots: Map<string, Node>;
}

// 4. RaceViewUI.ts - 竞赛视图（8小时）
@ccclass('RaceViewUI')
export class RaceViewUI extends Component {
    @property(ScrollView) leaderboardScrollView: ScrollView;
    @property(Label) currentValueLabel: Label;
}

// 5. AudioManager.ts - 音频管理（4小时）
@ccclass('AudioManager')
export class AudioManager extends Component {
    static getInstance(): AudioManager;
    playBgm(clipName: string): void;
    playSfx(clipName: string): void;
}
```

#### ✅ 微信小游戏
```javascript
// 1. CanvasRenderer.js - 渲染器（4小时）
class CanvasRenderer {
    clear();
    drawText(text, x, y, options);
    drawRect(x, y, width, height, options);
    drawButton(x, y, width, height, text);
    drawPlayerDot(x, y, player);
}

// 2. UIManager.js - UI管理（8小时）
class UIManager {
    switchView(viewName);
    renderStartup();
    renderAvatarCreation();
    renderGalaxyView();
    renderRaceView();
}

// 3. AudioManager.js - 音频管理（4小时）
class AudioManager {
    playBgm(src);
    playSfx(src);
    setBgmVolume(volume);
}
```

---

## 🎵 音效触发时机

```javascript
// UI交互
'click.wav'          -> 所有按钮点击
'page_switch.wav'    -> 场景切换

// 游戏事件
'group_join.wav'     -> 加入组队成功
'impact.wav'         -> 执行冲击
'score_up.wav'       -> 价值上升
'score_down.wav'     -> 价值下降

// 系统提示
'warning.wav'        -> 分数过低警告
'achievement.wav'    -> 排名提升
```

---

## 📐 屏幕适配方案

### Cocos Creator
```typescript
// Canvas设置
FitHeight   -> 固定高度，适配竖屏
FitWidth    -> 固定宽度，适配横屏
ShowAll     -> 显示全部内容

// Widget组件自动适配
widget.isAlignTop = true;
widget.isAlignBottom = true;
widget.top = 0;
widget.bottom = 0;
```

### 微信小游戏
```javascript
// 获取屏幕尺寸
const { screenWidth, screenHeight } = wx.getSystemInfoSync();

// 设计尺寸
const DESIGN_WIDTH = 750;
const DESIGN_HEIGHT = 1334;

// 计算缩放
const scaleX = screenWidth / DESIGN_WIDTH;
const scaleY = screenHeight / DESIGN_HEIGHT;
const scale = Math.min(scaleX, scaleY);

// 转换坐标
function toScreen(designX, designY) {
    return {
        x: designX * scale,
        y: designY * scale
    };
}
```

---

## 🎨 颜色配置

```javascript
const COLORS = {
    // 基础色
    background: '#000000',   // 黑色背景
    foreground: '#FFFFFF',   // 白色前景
    
    // 强调色
    primary: '#FFD700',      // 金色（榜首、按钮）
    success: '#4CAF50',      // 绿色（向上、成功）
    danger: '#F44336',       // 红色（向下、警告）
    warning: '#FF9800',      // 琥珀色（加成）
    
    // 辅助色
    gray: '#9E9E9E',         // 灰色（禁用）
    blue: '#2196F3',         // 蓝色（信息）
    
    // 透明度
    overlay: 'rgba(0,0,0,0.8)',  // 面板背景
    glow: 'rgba(255,215,0,0.5)'  // 光晕效果
};
```

---

## 📦 资源文件规范

### 图片资源
```
格式：PNG-24（带透明）
命名：小写+下划线 (button_normal.png)

尺寸：
- 按钮：200x60px
- 头像：100x100px
- 特效：根据需要
```

### 音频资源
```
BGM：
- 格式：MP3
- 比特率：96-128kbps
- 采样率：44.1kHz
- 时长：2-3分钟

SFX：
- 格式：WAV
- 比特率：16-bit
- 采样率：22.05kHz
- 时长：0.1-1秒
```

---

## 🐛 常见问题速查

### Q: Cocos场景是空的？
```typescript
// 解决方案：
1. 在场景中创建Canvas节点
2. 挂载GameController组件
3. 创建UI节点并挂载UI组件
```

### Q: 微信小游戏白屏？
```javascript
// 检查清单：
1. game.js中是否导入game-adapter
2. Canvas是否正确创建
3. 是否有JavaScript错误
4. 检查console.log输出
```

### Q: 音效不播放？
```javascript
// Cocos版本：
1. 检查音频文件路径
2. 确认AudioManager已初始化
3. 检查音量设置

// 微信版本：
1. 确认音频文件在正确路径
2. 检查wx.createInnerAudioContext是否成功
3. 查看onError回调
```

### Q: 触摸事件无响应？
```javascript
// 检查：
1. 触摸区域坐标是否正确
2. 是否正确注册touchHandlers
3. isPointInRect函数逻辑
4. 事件是否被其他元素遮挡
```

---

## 🔗 快速链接

### 文档
- [UI视觉音效添加指南](./UI视觉音效添加指南.md) - 完整实现指南
- [资源制作和实现路线图](./资源制作和实现路线图.md) - 开发计划
- [Cocos版本README](./cocos-version/README.md) - Cocos详细文档
- [微信版本README](./wechat-minigame/README.md) - 微信详细文档

### 工具
- [Cocos Creator](https://www.cocos.com/creator) - 游戏引擎
- [微信开发者工具](https://developers.weixin.qq.com/minigame/dev/devtools/download.html) - 微信开发
- [GIMP](https://www.gimp.org/) - 图像编辑
- [Audacity](https://www.audacityteam.org/) - 音频编辑

### 资源
- [Freesound](https://freesound.org/) - 免费音效
- [OpenGameArt](https://opengameart.org/) - 游戏素材
- [Google Fonts](https://fonts.google.com/) - 免费字体

---

## ⏱️ 开发时间估算

```
第一周（5天）：
Day 1: Cocos启动+头像创建
Day 2: Cocos主游戏界面
Day 3: 微信完整UI
Day 4: 组队和救援系统
Day 5: 音效系统

第二周（5天）：
Day 6: 视觉优化
Day 7: 测试和修复
Day 8: 资源优化
Day 9: 性能调优
Day 10: 发布准备

总计：60-80小时
```

---

## ✅ 每日检查清单

### 开发开始
- [ ] 拉取最新代码 `git pull`
- [ ] 检查分支 `git branch`
- [ ] 备份重要文件

### 开发中
- [ ] 定期保存工作
- [ ] 及时提交代码 `git commit`
- [ ] 运行测试验证功能
- [ ] 记录遇到的问题

### 开发结束
- [ ] 提交所有更改 `git add . && git commit`
- [ ] 推送到远程 `git push`
- [ ] 更新开发日志
- [ ] 规划明天任务

---

**记住：边开发边测试，小步快跑！** 🏃‍♂️

祝开发顺利！🎉
